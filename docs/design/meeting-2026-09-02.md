# ZAOstock design meeting - Wednesday 2 September 2026

Prepared 2026-08-27 by the DESIGN lane, from Zaal's "design Wednesday"
(typed 21:2x). The orchestrator is putting the invite on his calendar; time,
length and attendees beyond Zaal are UNSET. Suggested: 45 minutes, Zaal,
Candy (owns the identity), whoever is driving SITE that week.

Where the calendar sits: the day after the lineup reveal (1 September), the
day before the musician submission cutoff (3 September), 31 days before the
festival. Every decision below is one SITE is otherwise guessing at.

## Pre-reads, in order (20 minutes)

1. `public/design/fonts.html` and `public/design/colors.html` - open both in a
   browser from disk. This is the system rendered, not described.
2. `DESIGN.md` - the contract. Read "Decisions log" last.
3. `docs/design/before/design-review-2026-08-27.md` - what the live site is
   today, D+, and the twelve findings.
4. `docs/design/redesign-2026-08-28.md` - skim the build order and the two
   tables at the bottom.

## 1. Decisions DESIGN.md leaves to humans (20 minutes)

Each is an agent default today. Each needs one typed line from Zaal or Candy.
Recommended answer first.

| # | Decision | Recommended | Why | Who |
|---|---|---|---|---|
| 1 | **Fonts.** Boogaloo (display), Rubik (body), Space Mono (labels) - Candy's three, confirmed as the web set | Yes, confirm | They are her package; the previews show them doing every job the site has. Any change re-opens the whole scale | Candy, Zaal |
| 2 | **Colour: primary button ground.** red-600 `#B93826`, not the token's red-500 | Yes | Paper on red-500 measures 4.2:1, under the 4.5:1 a 14px bold label needs; red-600 measures 5.2:1. Red-500 stays the brand red everywhere else | Candy |
| 3 | **Colour: secondary and muted text.** Ink at 70% and 66% over paper, not 62% and 46% | Yes | Her mixes measure 4.3:1 and 2.8:1; eyebrows are 12px bold and need 4.5:1 | Candy |
| 4 | **Colour: the H1 accent word** ("stock") in gold-600 `#A8721C`, not gold-400 | Yes | gold-400 on paper is 1.7:1. gold-600 is 3.3:1, which passes only at display size, which is the only place it is used | Candy |
| 5 | **Dark mode.** None for 3 October | Yes | Paper-first identity, no dark reference, one less thing to verify | Zaal |
| 6 | **Motion.** Zero continuous animation; press and hover only | Yes | The live homepage would not reach idle in three renderers | Zaal |
| 7 | h4 in Rubik 800 rather than Boogaloo; 96px hero-to-first-section gap | Yes | Small; listed so nothing is unflagged | Candy |

### The two UNSET in DESIGN.md

| | What | Ask |
|---|---|---|
| A | **The ICM brand boxes** (The ZAO and ZAOstock, `useicm.com/api/objects/...`). Both timed out from every path on 27 Aug. Nothing from them is in the system | Is `useicm.com` down or moved? If the boxes hold voice or fact rules the site must follow, paste them into `docs/brand/` before SITE writes copy |
| B | **The OG / social card image.** No image exists; every shared link renders with no picture today | Approve the obvious one: the badge on paper at 1200x630, generated with `next/og`. Twenty minutes for SITE once said yes |

## 2. The before screenshots (5 minutes)

**UNSET.** Three renderers failed on 27 Aug; the verbatim errors are in
`docs/design/before/README.md`. The audit was done from the served HTML of
thirteen routes instead, so the grades stand, but "responsive" and
"interaction" are marked not measured.

Ask: one person with a Chrome that can load zaostock.com captures `/`,
`/program`, `/sponsor`, `/musicians`, `/apply`, `/pitch` at 375, 768 and
1440 **before SITE's shell deploys**, and drops them in
`docs/design/before/` as `<route>-<width>.png`. After the deploy the old site
is gone and there is no before.

Zaal's call: is that worth twenty minutes of someone's Tuesday, or do we accept
the HTML-only baseline and move on. Recommended: capture them; the after-state
comparison is the proof the redesign worked.

## 3. The redesign order (10 minutes)

From the spec, twelve steps, each its own commit and screenshot pair:

1. Shell - tokens, three fonts, Header, Footer, twelve primitives,
   `festival.ts` extended. Nothing visible changes.
2. `/` - seven sections, not fourteen.
3. `/program` - four H2 blocks, noon start, "in booking" for the evening.
4. `/press` - new; the paper has already been told it exists.
5. `/sponsor` - one page; `/sponsor/deck` and `/pitch` redirect into it.
6. `EntryPage` - `/musicians`, `/artists`, `/event-organizers`.
7. The four forms.
8. `/ellsworth`, `/acadia`.
9. `/festivals`, `/zaoville`.
10. `/donate`.
11. One-pagers, artist profiles, privacy, `llms.txt`.
12. Delete the old components and every old hex literal; re-run the audit.

Questions for the room: does step 2 or step 4 go first (the email needs both;
`/press` is smaller). Is anything in steps 8-11 allowed to slip past 3 October
if time runs out. Recommended: order as written; 8-11 may ship on the old shell
if they must, because none of them is in the email.

## 4. The eight fields only Zaal can type (10 minutes)

Not design decisions, but they render on the pages the meeting is about, and
this is the room he is in.

1. The five package prices, or "ask" as the policy (`/sponsor`).
2. Sponsor-an-artist price (`/sponsor`; doc 2325 says $500, unconfirmed).
3. Where the fifth confirmed act plays now that a noon start leaves four
   45-minute slots (`/program`).
4. Set length: 20, 25 or 45 minutes are all on disk (`/musicians`).
5. Pro Ticket, $50, 20 spots: keep on `/donate` or drop.
6. Contact address: `info@thezao.com` or `zaal@thezao.com`, everywhere.
7. Whether ENTERACT may be named as the payment route (`/sponsor`).
8. Advisor board names, if that block survives (`/sponsor`).

## 5. What the meeting should end with

A line per row in section 1, a yes or no on the screenshots, an order for
steps 2 and 4, and as many of the eight fields as Zaal will type in the room.
Each typed line goes into `DESIGN.md`'s decisions log with the date, and the
matching UNSET in the spec is replaced the same day.

## Not for this meeting

Partner logos (zero of eight on disk) and photographs (deck slide 3) are asset
chases, not decisions. The parklet hours, the insurance certificate and the
45-day notice are the City's. All are listed at the bottom of the spec with
owners.
