node_modules/
.next/
out/
.env
.env.local
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.serena/
scripts/team-codes.local.json
.vercel
.env*
.gstack/

# Local agent session scratch. .claude/LAST_MESSAGE.md holds whatever was last
# typed in this folder, and this repo is PUBLIC, so it must never be committed.
.claude/
